# Skill examples

Worked examples referenced from `SKILL.md`. Each file is a standalone,
runnable demonstration of one event-vocabulary pattern (linear,
evolutionary, self-improving, tree-search, quality-diversity). The
real-LLM eval at `hutch-py/tests/test_skill_eval.py` exercises the same
patterns by prompting an LLM to reproduce them.
